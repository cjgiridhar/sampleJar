apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

//  create a single Jar with all dependencies
jar {
    manifest {
        attributes 'Main-Class': 'sample.sample'
    }
}

dependencies {
    //testCompile group: 'junit', name: 'junit', version: '4.11'
}